deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

machine:
  environment:
      DJANGO_SETTINGS_MODULE: play.settings.testing
test:
  override:
      - python manage.py test
